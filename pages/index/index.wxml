<!--index.wxml-->
<view class="container">
 <map id="map" bindtap="{{bindtap}}" controls="{{controls}}" bindcontroltap="controltap"  polyline="{{polyline}}" class='map' show-location longitude="{{location.longitude}}" latitude="{{location.latitude}}"></map> 
<text>{{text}}</text>
</view>